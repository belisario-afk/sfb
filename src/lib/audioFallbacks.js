// Basic placeholder (your earlier build error occurred because this file was missing).
// If you later use fallback tones/noise, you can expand this.
export function playFallbackBeep(label, durationSeconds) {
  console.log('[FallbackAudio] (noop beep) for', label, durationSeconds, 's');
}
export function playFallbackNoise(label, durationSeconds) {
  console.log('[FallbackAudio] (noop noise) for', label, durationSeconds, 's');
}